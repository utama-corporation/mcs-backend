name: Deploy to Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: git pull origin master
        working-directory: D:\backend\mcs_backend

      - name: Deploy with Docker
        run: docker compose up -d --build
        working-directory: D:\backend\mcs_backend
