# IP server MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=mcs_new

# JWT secret used to sign/verify auth tokens
SECRET_KEY=change_me

# Port the app listens on (optional, check app.js for default)
PORT=3000
